[core]
	pager = diff-so-fancy | less --tabs=4 -RFX
[push]
	autoSetupRemote = true
[alias]
	pull = pull --merge --autostash
[diff]
	tool = vscode
[difftool "vscode"]
	cmd = code --wait --diff $LOCAL $REMOTE
